# Python 3.14 mos kelgan versiyalar.
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0.36
alembic>=1.14

# DB drivers
aiosqlite>=0.20      # SQLite async (dev default)
asyncpg>=0.30        # Postgres async (production uchun)
psycopg[binary]>=3.2 # Postgres sync (Alembic uchun)

# Validation
pydantic>=2.11
pydantic-settings>=2.6
email-validator>=2.2

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0,<5.0  # passlib 1.7.4 bcrypt 5.x bilan mos kelmaydi
python-multipart>=0.0.19

# Optimization (Phase 4'da)
ortools>=9.15

# HTTP
httpx>=0.28
python-dotenv>=1.0

# Dev/test
pytest>=8.3
pytest-asyncio>=0.25
